<h2 id="title">
  {{#link-to 'index'}}
    old-hash
  {{/link-to}}
</h2>
{{#if session.isGithubAuthenticated}}
  <div>Logged In</div>
{{else}}
  <button {{action 'login'}}>Login</button>
{{/if}}

{{outlet}}
